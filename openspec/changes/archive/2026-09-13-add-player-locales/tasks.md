## 1. Shared process (do this first; nothing else starts until the checker exists)

- [x] 1.1 Write `build/locale-glossary.md` locking proper names for all twelve languages (Scribe, Lectern, Scriptorium, Chalkboard, Assignment Desk, Inbox, Task Notice, Notebook, Clockmaker's Notebook, Clay/Wax Tablet, Guest Book, Pinned Task HUD, History, Transcribe). Spanish uses C4B's names; Portuguese uses Arquimago's names; greenfield languages pick one equivalent each. This glossary covers in-world nouns only — UI action/button/tab labels (e.g. "Add Item Tracker", "Task Editor", "Settings", "Timer") are not hand-listed here; they're generated and enforced by the checker in 1.2. Verify the file lists every English name in design.md Decision 2.
- [x] 1.2 Add `build/check-locales.py` that validates every `src/Mod/assets/scribe/lang/*.json` except `en.json` against the spec (no extra keys, all required keys present, optional flavor contiguous from `-0`, `{n}` indexes match, VTML tags and `handbook://` targets match, no `_comment-*`, handbook-length values not copied English) and checks `assets/game/lang/<code>.json` has `worldattribute-scribeClockmakerRequiresTrait` for `en` plus each shipped locale. Also generate a UI-label cross-reference table from `en.json` (handbook/craftinginfo `<strong>`/`<em>`/quoted spans whose exact English text matches another key's English value — button/tab/action labels) and enforce, per locale, that each mapped span's translation equals that locale's own translated value of the referenced label key. Verify it exits non-zero on current `pt-br.json` (missing keys + stale extras) before any locale is rewritten; verify the generated table includes at least the `Add Item Tracker` / `Task Editor` / `Settings` / `Timer` cross-references (`scribe-gui-addtracker`, `scribe-gui-switch-to-editor`, `scribe-hud-questprompt-settings-button`, `scribe-gui-nav-timer`); verify the checker fails when one of those spans is deliberately mistranslated out of sync with its label key in a test file.
- [x] 1.3 Hook `python3 build/check-locales.py` as a stage in `build/verify.sh` (before or after Core tests; do not edit `.github/workflows`). Verify `./build/verify.sh --no-restage` names the locale-check stage when the script fails.

## 2. Seed locales (meaning audit first, then fill gaps)

- [x] 2.0 From `git log -p -- src/Mod/assets/scribe/lang/en.json`, list keys whose English value changed since C4B's 1.4.0-rc.1 vintage and since the last real Arquimago `pt-br` translation (not an English-fallback key-mirror commit). Verify the lists include at least `tablet-full` and `craftinginfo-scribe-getting-started-text`.
- [x] 2.1 Ingest C4B Spanish from `~/Downloads/C4B_Traducciones-ES_MODS_246.10.2.zip` (`assets/scribe/lang/es-es.json` only; do not commit the pack): drop comment-keys and extras, strip injected `<font>` markup, **meaning-audit every overlapping key** (walk the 2.0 list, then read remaining handbook/craftinginfo essays), re-translate drift (including Getting Started and `tablet-full` 15), keep C4B names/wording where English is unchanged, fix timer start/stop false friends, fill missing 1.4.x keys, move the Clockmaker line to `src/Mod/assets/game/lang/es-es.json`. Verify `python3 build/check-locales.py` accepts `es-es.json`, that it has no `game:`-prefixed keys, and that Getting Started describes the current mod (tablets, chalkboard, assignments), not the old Lectern-only intro.
- [x] 2.2 Refresh `src/Mod/assets/scribe/lang/pt-br.json` from current `en.json`: **meaning-audit every overlapping Arquimago key** the same way (2.0 list + handbook overlap), keep values whose English meaning is unchanged, re-translate drift (including `tablet-full`), fill the ~242 missing keys, drop extras, add `src/Mod/assets/game/lang/pt-br.json`. Verify the checker accepts `pt-br.json` and that `tablet-full` no longer says 10.

## 3. Greenfield locales (parallel after 1.x and 2.x; each from current `en.json` + the glossary, never from `pt-br` / `es-es`)

Short History flavor only (`scribe-mob-death-0`, optional `-1` literal, `scribe-pvp-verb-generic-0`, the two PvP message templates). Also write `src/Mod/assets/game/lang/<code>.json` for the Clockmaker line. Verify each file with `python3 build/check-locales.py`.

- [x] 3.1 Add `src/Mod/assets/scribe/lang/ru.json` (Russian) and `src/Mod/assets/game/lang/ru.json`; checker accepts them.
- [x] 3.2 Add `src/Mod/assets/scribe/lang/uk.json` (Ukrainian) and `src/Mod/assets/game/lang/uk.json`; checker accepts them.
- [x] 3.3 Add `src/Mod/assets/scribe/lang/pl.json` (Polish) and `src/Mod/assets/game/lang/pl.json`; checker accepts them.
- [x] 3.4 Add `src/Mod/assets/scribe/lang/de.json` (German) and `src/Mod/assets/game/lang/de.json`; checker accepts them.
- [x] 3.5 Add `src/Mod/assets/scribe/lang/zh-cn.json` (Simplified Chinese) and `src/Mod/assets/game/lang/zh-cn.json`; checker accepts them.
- [x] 3.6 Add `src/Mod/assets/scribe/lang/ja.json` (Japanese) and `src/Mod/assets/game/lang/ja.json`; checker accepts them.
- [x] 3.7 Add `src/Mod/assets/scribe/lang/cs.json` (Czech) and `src/Mod/assets/game/lang/cs.json`; checker accepts them.
- [x] 3.8 Add `src/Mod/assets/scribe/lang/sv-se.json` (Swedish; filename `sv-se`, not `sv`) and `src/Mod/assets/game/lang/sv-se.json`; checker accepts them.
- [x] 3.9 Add `src/Mod/assets/scribe/lang/fr.json` (French) and `src/Mod/assets/game/lang/fr.json`; checker accepts them.
- [x] 3.10 Add `src/Mod/assets/scribe/lang/it.json` (Italian) and `src/Mod/assets/game/lang/it.json`; checker accepts them.

## 4. Docs, credits, playtest

- [x] 4.1 Add CHANGELOG Unreleased notes: twelve shipped locales; C4BR3R4 Spanish seed (overlapping keys meaning-audited against current English, then filled); Arquimago Portuguese refreshed the same way; if C4B's pack is still installed last-loaded `es-es.json` wins. Verify the entry names both translators.
- [x] 4.2 Credit C4BR3R4 on `docs/media/wiki/Home.md` next to Arquimago. Verify both names appear under translation credits.
- [x] 4.3 Add TESTING.md items (via the what-to-test loop, or as tasks here): (a) client language Russian — Lectern chrome + a handbook page are Russian; (b) Brazilian Portuguese — tablet-full says 15, not 10, and Getting Started matches the current tour; (c) Spanish — C4B Lectern name, Getting Started is the current article (not Lectern-only), plus a previously-missing Inbox/Task Notice string; (d) Japanese or Chinese — dialog and handbook glyphs render (record tofu as a follow-up, do not block); (e) a short-pool locale — a new creature-death History row is one translated sentence, not an English joke. Verify the items exist under this change's heading.
- [x] 4.4 Restage (`./build/restage.sh`) so the new lang files are in the staged mod, and run `python3 build/check-locales.py` as the final gate. Verify the staged zip/folder contains all twelve `scribe/lang` files plus matching `game/lang` files.
