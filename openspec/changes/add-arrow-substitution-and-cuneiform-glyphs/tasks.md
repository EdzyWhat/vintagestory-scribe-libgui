## 1. Typed-arrow substitution (ships ungated in v1.1)

- [x] 1.1 Add a pure, game-API-free digraph helper (fixed two-entry table `->`→`→` U+2192, `<-`→`←` U+2190) exposing a completion check — e.g. `bool TryCompleteArrow(char justTyped, char before, out char arrow)`. Keep it tiny and explicit (no regex/config); place it where it can be unit-tested without a game install. → `src/Core/ScribeArrowDigraph.cs` (`TryComplete` table + `TryApply` full text/caret transform, both pure).
- [x] 1.2 Hook the helper into `ScribeMultilineFieldState` on the typed-character path (`OnKeyChar` → `Insert`): when the just-typed character completes a digraph with the character immediately before the caret, replace the two-char run with the single arrow and set the caret immediately after the arrow (net +1). Examine only the two code units before the caret. → wired in `OnKeyChar` via `ScribeArrowDigraph.TryApply`, gated on no active selection.
- [x] 1.3 Confirm paste (`Paste`/clipboard `Insert`) stays literal — the transform must fire only on single-keystroke completion, not on bulk paste (design D3). → transform lives only on the `OnKeyChar` typed path; `Paste`/`Insert` are untouched.
- [x] 1.4 Verify the substitution reaches all four surfaces (Lectern, Notebook, Clockmaker's Notebook, wet Tablet) and both task and note text through the shared State — no per-surface wiring needed. → single seam in the shared `ScribeMultilineFieldState`; both render paths (normal + cuneiform) run through it.
- [x] 1.5 Add unit tests for the helper: digraph at end of buffer, digraph mid-text with trailing content, digraph right after an existing arrow, and non-triggers (`<` alone, `-` alone, `- >` with a space, out-of-scope `<->`). → `tests/Core.Tests/ScribeArrowDigraphTests.cs` (19 cases).
- [~] 1.6 Run `dotnet test` (Core suite) green; then in-game smoke: type `->` and `<-` in a lectern task and a notebook note, confirm arrows appear, caret lands after the arrow, and reopen shows the stored arrow. → Core suite green (358/358); **in-game smoke pending** (needs a game launch).
- [x] 1.7 Arrow font fallback for the normal (non-cuneiform) editor render. The editor field paints through `PaintingContext.DrawText` (raw single-`SKFont` `Canvas.DrawText`, NO shaping/fallback), so a stored ←/→ that the active task font lacks rendered as tofu — verified the bundled Noto Sans/Noto Serif/La Belle Aurore subsets carry no arrow glyph (Scapholene/Caudex do). Added `src/Mod/ScribeGlyphFallback.cs`: draws/measures a line, redirecting ONLY an arrow the active font can't render (and the fallback can) to "Cormorant Unicase" (bundled by the `gui` dep → deterministic, unlike OS `MatchCharacter`). No-arrow lines take a single-draw fast path identical to `context.DrawText`. Wired into `ScribeMultilineFieldRender.PaintInternal` (both draw sites) + `MeasureWidth` (so caret/selection align with the redirected glyph). The READ view already falls back (its stock `Text` widget draws via the shaping `CanvasDrawExtensions.DrawText`), so no change there. Mod builds 0 warnings.

## 2. Cuneiform glyph coverage (art-gated on glyph-forge; clean drop-in)

- [x] 2.1 GATE — confirm the four glyph stroke files are drawn in `glyph-forge` (slugs `arrowleft`/`arrowright`/`lessthan`/`greaterthan`) and the bundle regenerated via `tools/build_glyphs_bundle.py` (its `characterCount` reaches 58). → drawn, regenerated (58/58), committed to glyph-forge `main` (`c46c636`); arrows 3-stroke, `<`/`>` 2-stroke.
- [x] 2.2 Copy the regenerated `glyphs-1.json` into the mod at `src/Mod/assets/scribe/textures/fonts/cuneiform-glyphs-1.json`; confirm its `characterCount` is 58 and the four new characters (`←`, `→`, `<`, `>`) are present. → copied; verified purely additive (+4 chars, all 54 prior glyphs byte-identical).
- [x] 2.3 Update `tests/Core.Tests/CuneiformTests.cs` — `Parse_ShippedBundle_ContainsAllAuthoredCharacters`: bump the count assertion 54 → 58 (and adjust the "18 punctuation = 54" comment/derivation accordingly). → assertion 58, comment updated (22 punctuation/symbols), plus `Contains` checks for `←`/`→`/`<`/`>`.
- [x] 2.4 Confirm `CuneiformLineLayout.Aliases` is NOT modified — the four characters are authored glyphs, not aliases; verify no alias entry exists for `←`/`→`/`<`/`>`. → `CuneiformLineLayout.cs` untouched.
- [~] 2.5 Run `dotnet test` green (count assertion now 58); in-game smoke via `.cuneiform ← → < >` and by typing them into a wet tablet — confirm authored strokes render (not the safe gap). → Core suite green (358/358); **in-game `.cuneiform` smoke pending** (needs a game launch).

## 3. Release bookkeeping (fold into v1.1)

- [x] 3.1 Add a CHANGELOG entry under `[Unreleased]`/`[1.1.0]` for the typed-arrow substitution and (when 2.x lands) the four new cuneiform glyphs. → two `[Unreleased] > Added` bullets: typed arrows (incl. the font fallback) + cuneiform arrow/comparison glyphs.
- [x] 3.2 Tick the corresponding `docs/vnext-ideas.md` items (§4.5 substitution, §1.7 arrows, §1.6 `<`/`>`) as delivered when both halves have shipped. → all three headers marked ✅ DELIVERED (v1.1, `add-arrow-substitution-and-cuneiform-glyphs`).
