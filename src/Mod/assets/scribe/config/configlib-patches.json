{
  "version": 1,
  "file": "scribe-client-config.json",
  "settings": [
    {
      "code": "VisibleListHeight",
      "type": "float",
      "default": 400,
      "comment": "Fixed viewport height for the lectern's scrollable row-list region.",
      "range": { "min": 100, "max": 1000 }
    },
    {
      "code": "RowSpacing",
      "type": "float",
      "default": 14,
      "comment": "Vertical gap between rows in the lectern's row list.",
      "range": { "min": 0, "max": 60 }
    },
    {
      "code": "TopContentGap",
      "type": "float",
      "default": 20,
      "comment": "Vertical gap between the lectern's title bar and its first row.",
      "range": { "min": 0, "max": 100 }
    },
    {
      "code": "RowListWidth",
      "type": "float",
      "default": 500,
      "comment": "Row-list width, shared by the lectern's read and editor views.",
      "range": { "min": 100, "max": 800 }
    }
  ]
}
