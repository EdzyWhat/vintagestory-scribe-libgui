{
	code: "scribeassignmentdesk",
	class: "BlockAssignmentDesk",
	entityclass: "AssignmentDesk",

	// PLACEHOLDER MODEL (add-custom-models-tasknotice-desk-inbox, 2026-09-03 direction): now a locally-
	// owned shape, cloned unmodified from the Scriptorium's own shape + Blockbench source (`.bbmodel`)
	// so a real art pass can open and edit it directly instead of hand-editing raw shape JSON. Renders
	// identical to the Scriptorium until that art pass diverges it. A JSON-only swap will retarget this
	// once real art exists; no code change needed.
	shape: { base: "scribe:block/assignmentdesk/assignmentdesk" },

	// The cloned shape pulls texture keys from this dict at render time (mirrors scriptorium.json's own
	// — a shape's own embedded `textures` are editor-only for blocks). Generated by
	// build/gen-scriptorium-shape.py originally; kept identical here pending real art.
	textures: {
		"charred": { base: "scribe:block/assignmentdesk/charred" },
		"ebony": { base: "scribe:block/assignmentdesk/ebony" },
		"ebony-ornate": { base: "scribe:block/assignmentdesk/ebony-ornate" },
		"opened-normal": { base: "scribe:block/assignmentdesk/opened-normal" },
		"normal-orangebrown": { base: "scribe:block/assignmentdesk/normal-orangebrown" },
		"feather": { base: "scribe:block/assignmentdesk/feather" },
		"bituminous": { base: "scribe:block/assignmentdesk/bituminous" },
		"quartz": { base: "scribe:block/assignmentdesk/quartz" },
		"brass": { base: "scribe:block/assignmentdesk/brass" },
		"blank": { base: "scribe:item/tasknotice/blank" },
		"blank-tie": { base: "scribe:item/tasknotice/blank-tie" },
		"filled": { base: "scribe:item/tasknotice/filled" },
		"filled-tie": { base: "scribe:item/tasknotice/filled-tie" }
	},

	creativeinventory: { "general": ["*"], "decorative": ["*"] },

	blockmaterial: "Wood",
	lightAbsorption: 0,
	sidesolid: { all: false },
	sideopaque: { all: false },
	faceCullMode: "NeverCull",
	replaceable: 500,
	resistance: 2,

	collisionbox: { x1: 0.15, y1: 0, z1: 0.15, x2: 0.85, y2: 1.0, z2: 0.85 },
	selectionbox: { x1: 0.15, y1: 0, z1: 0.15, x2: 0.85, y2: 1.0, z2: 0.85 },

	sounds: {
		place: "block/planks",
		hit: "block/planks",
		break: "block/planks",
		walk: "walk/wood"
	},

	guiTransform: {
		rotation: { x: -22.6, y: 142 },
		origin: { x: 0.5, y: 0.6, z: 0.5 },
		scale: 1
	}
}
