{
	code: "scribeassignmentdesk",
	class: "BlockAssignmentDesk",
	entityclass: "AssignmentDesk",

	// PLACEHOLDER MODEL (add-assignment-and-quest-support §13.1, 2026-08-30 direction): reuses the
	// Lectern's own shape wholesale (a writing-desk stand is a closer visual fit for "Assignment Desk"
	// than the Scriptorium cabinet this used to clone) ahead of the Desk's own dedicated art. The shape
	// self-resolves its textures (scribe:block/lectern/*), same as lectern.json's own blocktype — no
	// textures override needed here either. A JSON-only swap will retarget this once real art exists;
	// no code change needed.
	shape: { base: "scribe:block/lectern/lectern" },

	creativeinventory: { "general": ["*"], "decorative": ["*"] },

	blockmaterial: "Wood",
	lightAbsorption: 0,
	sidesolid: { all: false },
	sideopaque: { all: false },
	faceCullMode: "NeverCull",
	replaceable: 500,
	resistance: 2,

	collisionbox: { x1: 0.1817, y1: 0, z1: 0.1875, x2: 0.8188, y2: 1.2563, z2: 0.8125 },
	selectionbox: { x1: 0.1817, y1: 0, z1: 0.1875, x2: 0.8188, y2: 1.2563, z2: 0.8125 },

	sounds: {
		place: "block/planks",
		hit: "block/planks",
		break: "block/planks",
		walk: "walk/wood"
	},

	guiTransform: {
		rotation: { x: -22.6, y: 142 },
		origin: { x: 0.5, y: 0.6, z: 0.5 },
		scale: 1
	}
}
